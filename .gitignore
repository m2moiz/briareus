# macOS
.DS_Store

# Python bytecode cache
__pycache__/
*.pyc

# Logs (regenerable provisioning/run output)
logs/
*.log

# Local Dolt working data (beads tracks issues via the Dolt server)
.dolt/

# Agent context — shared with the team
# (AGENTS.md and CLAUDE.md are tracked on purpose)

# Local tooling / workflow state — kept out of the repo
.beads/
.claude/
.planning/
